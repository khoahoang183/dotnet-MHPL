@model PagedList.IPagedList<Model.EF.SOLUONGDANGKY>
@using PagedList.Mvc;

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Danh Sách Số Lượng Đăng Ký</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<div class="col-lg-12">
    <div class="panel panel-default">
        <!-- /.panel-heading -->
        <div class="panel-body">
            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <!--------------------------------------------- Search And Add ------------------------------------------------->
                    <div class="form-group">
                        @using (Html.BeginForm("Index", "SoLuongDangKy", FormMethod.Get))
                        {
                            <label>Nhập Mã Đại Lý:</label>
                            <input type="text" , name="searchString">
                            <button type="submit" class="btn btn-primary">Tìm Kiếm</button>
                        }
                        <a href="/SoLuongDangKy"><button type="submit" class="btn btn-primary">Làm Mới</button></a>
                        <a href="/SoLuongDangKy/Create"><button type="button" class="btn btn-warning">Thêm Mới</button></a>
                    </div>
                    <!------------------------------------------- Loading Edit Delete ----------------------------------------------->
                    <thead>
                        <tr>
                            <th>Mã Số Lượng</th>
                            <th>Mã Đại Lý</th>
                            <th>Ngày</th>
                            <th>Số Lượng Đăng Ký</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr id="row_@item.MASLDK">
                                <td> @item.MASLDK</td>
                                <td> @item.MADL</td>
                                <td> @item.NGAY</td>
                                <td> @item.SLDANGKY</td>
                                <td>
                                    <a href="/SoLuongDangKy/Edit/@item.MASLDK"><button type="button" class="btn btn-primary">Sửa</button></a>
                                   
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))
            </div>
            <!-- /.table-responsive -->
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.panel -->
</div>